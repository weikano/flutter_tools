import 'package:flutter/material.dart';

const baseTextStyle = TextStyle(fontFamily: 'poet');

var itemTextStyle = baseTextStyle.copyWith(
  fontSize: 16,
  color: Colors.black45,
);
var sectionTextStyle = baseTextStyle.copyWith(
  fontSize: 18,
  color: Colors.white,
);
